<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.it.chaoshi.dao.ClassificationDao">
    <select id="getByPid" resultType="com.it.chaoshi.pojo.Classification">
        select * from leve where  parentId= #{parentId}
    </select>
</mapper>